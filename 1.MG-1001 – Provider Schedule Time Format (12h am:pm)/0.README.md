# MG-1001 — Provider Schedule Time Format (12h am/pm)

QA-артефакти для задачі з оновлення логіки форматування часу у медичній CRM-системі **MagendaMD**.

---

## Мета задачі

Потрібно уніфікувати відображення часу у всій системі, перевівши інтерфейс із 24-годинного формату на **12-hour format (am/pm)**.

Система повинна:

1. Відображати час у форматі `7:00 am` / `12:15 pm`
2. Якщо зазначено лише **start_time**, показувати тільки початковий час
3. Якщо задані **start_time** і **end_time**, відображати діапазон
   `7:00 am – 11:30 pm`
4. Усі модулі мають використовувати **однаковий формат часу**

---

## 📂 Структура цієї папки

### **1️⃣ Test Cases (TestRail)**

- Експортовані тест-кейси із TestRail
- Включають preconditions, steps, expected results
- Атомарні, структуровані, згруповані за секціями

### **2️⃣ Test Run (TestRail)**

- Результати виконання всього сьюта
- Статуси Passed / Failed
- Коментарі до failed-кейсів
- Додаткові вибіркові PDF для перегляду логіки помилок

### **3️⃣ Test Cases (Google Sheets)**

- Усі тест-кейси у PDF-форматі
- Розбиті за секціями (TC-1.x, TC-2.x, TC-3.x)
- Відображають ручний підхід до тест-дизайну

### **4️⃣ Bug Reports (Jira)**

- Виявлені дефекти під час виконання тестів
- Формат: Summary, Steps, Expected, Actual, Severity
- Охоплюють проблеми state-update, UI inconsistency, tooltip mismatch

### **5️⃣ Bug Reports (Google Sheets)**

- Додатково оформлені баги у зручному форматі для перегляду
- Мають повний опис та приклади відтворення

---

## Зона тестування (Testing Scope)

Тестування покриває 5 основних компонентів UI:

1. **Edit Provider In Calendar (Time Fields)**
   — Валідація Start / End у форматі 12h am/pm
2. **Scheduled Providers (Calendar card)**
   — Відображення часу після збереження графіка
3. **Provider Schedule Card (Main Calendar)**
   — Відображення одиночного часу та діапазону
4. **Sidebar Mini Calendar Tooltip**
   — Tooltip-format для single-time та range-time
5. **Date & Time Picker (“Add New Appointment”)**
   — Генерація списку годин відповідно до графіка провайдера

---

## Техніки тестування, що застосовувались

- Functional & UI testing
- Boundary Value Analysis
- State-transition behaviour
- Data-driven scenarios
- Regression & smoke testing
- Positive / negative flows
- Consistency validation across UI components

---

## Підсумки роботи

- Розроблено **17 структурованих тест-кейсів**
- Створено повноцінний Test Suite у TestRail
- Виконано Test Run з документуванням результатів
- Зібрано баг-репорти з аналізом причин
- Виявлено edge cases та state-update issues
- Репозиторій містить повний цикл QA-процесу для однієї фічі:

  **вимоги → тест-кейси → тест-ран → баги → результат**
